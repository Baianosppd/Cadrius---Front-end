# Conecta o Front-end à mesma rede do Back-end
networks:
  proxy_net:
    external: true

services:
  frontend:
    container_name: cadrius_frontend
    build: .
    # Força a usar o Vite e expor a porta corretamente
    command: npm run dev -- --host 0.0.0.0 --port 3000
    volumes:
      - ./src:/app/src
      - ./public:/app/public
      - /app/node_modules
    environment:
      - CHOKIDAR_USEPOLLING=true
    networks:
      - proxy_net
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=proxy_net"
      - "traefik.http.routers.cadrius-front.rule=Host(`app.cadrius.ia.br`)"
      - "traefik.http.routers.cadrius-front.entrypoints=web"
      - "traefik.http.services.cadrius-front.loadbalancer.server.port=3000"