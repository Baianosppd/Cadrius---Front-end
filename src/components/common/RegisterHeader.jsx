import styles from './RegisterHeader.module.css';

const RegisterHeader = () => {
    return (
        <div className={styles.header}>
            <span className={styles.logo}>Cadrius</span>
        </div>
    );
};

export default RegisterHeader;